<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.hdlmx.devops.dao.ImageRepositoryDao">

    <insert id="save">
      insert into imagerRepository(url, username, password, projectid)
      values (#{repository.url},#{repository.username},#{repository.password},#{repository.projectId})
    </insert>

</mapper>